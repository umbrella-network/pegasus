# Crypto

ETH-USD:
  discrepancy: 0.5
  precision: 2
  inputs:
    - fetcher:
        name: CryptoComparePriceWS
        params:
          fsym: ETH
          tsym: USD

BTC-USD:
  discrepancy: 0.5
  precision: 2
  inputs:
    - fetcher:
        name: CryptoComparePriceWS
        params:
          fsym: BTC
          tsym: USD

LINK-USD:
  discrepancy: 0.5
  precision: 2
  inputs:
    - fetcher:
        name: CryptoComparePriceWS
        params:
          fsym: LINK
          tsym: USD

BNT-USD:
  discrepancy: 0.5
  precision: 2
  inputs:
    - fetcher:
        name: CryptoComparePriceWS
        params:
          fsym: BNT
          tsym: USD

UNI-USD:
  discrepancy: 0.5
  precision: 2
  inputs:
    - fetcher:
        name: CryptoComparePriceWS
        params:
          fsym: UNI
          tsym: USD

BNB-USD:
  discrepancy: 0.5
  precision: 2
  inputs:
    - fetcher:
        name: CryptoComparePriceWS
        params:
          fsym: BNB
          tsym: USD

UMB-USD:
  discrepancy: 0.5
  precision: 2
  inputs:
    - fetcher:
        name: CoingeckoPrice
        params:
          id: umbrella-network
          currency: usd

# GVol

GVol-BTC-IV-28days:
  discrepancy: 0.5
  precision: 2
  inputs:
    - fetcher:
        name: GVolImpliedVolatility
        params:
          query: twentyEightDayIv
          sym: BTC

GVol-ETH-IV-28days:
  discrepancy: 0.5
  precision: 2
  inputs:
    - fetcher:
        name: GVolImpliedVolatility
        params:
          query: twentyEightDayIv
          sym: ETH
